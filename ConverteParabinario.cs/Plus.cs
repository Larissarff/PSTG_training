// Solicita os números ao usuário
        Console.WriteLine("Digite o primeiro número:");
        numero.Num1 = int.Parse(Console.ReadLine()); // Lê e converte a entrada para int

        Console.WriteLine("Digite o segundo número:");
        numero.Num2 = int.Parse(Console.ReadLine()); // Lê e converte a entrada para int
